if query_result.parameters:
    # MapComposite can be converted directly to dict like this
    params_dict = dict(query_result.parameters)
    # Convert any nested values to plain python types
    params_dict = {k: (dict(v) if hasattr(v, 'keys') else v) 
                   for k, v in params_dict.items()}
    print(f"[DEBUG] ✅ params_dict: {params_dict}")
